input {
    tcp {
        port => <%= @logstash_port %>
    }
    udp {
        port => <%= @logstash_port %>
    }
}

output {
    elasticsearch {
        hosts => "<%= @elasticsearch_host %>:<%= @elasticsearch_port %>"
    }
    stdout {}
}
